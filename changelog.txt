20160926
基本功能：
1、增加数字资源站、媒体资源站注册授权。
2、在日志查询页面增加不同模块日志打包下载功能。
3、增加创建及导入系统卷的后台配置文件脚本配置项。
SYNC模块：
1、可以进行A2B2C三台设备的级联远程复制，实现两地三中心的备份。
2、远程复制增加对网络断开和接口调用超时的不同处理。
Oracle模块：
1、修改Oracle无代理备份存储数据自动清理错误的bug（RAC环境尚待测试）。
CDP分离器：
1、Linux分离器修改CDP镜像管理界面上保护时显示“正在恢复”的bug。
2、Linux分离器增加了对 Multipath 设备的 3.10.x 内核版本的支持。
3、Windows分离器修改保护过程中有可能造成被保护盘与镜像盘对应关系丢失及蓝屏的Bug。

20160802
基本功能：
1、增加卷组扩容功能。
2、增加内存智能控制功能。
3、解决设备启动时日志记录获取数据库名失败的bug。
4、出厂检测程序去掉J选项。
SYNC模块：
1、由于现在公网多数禁止ping主机，远程复制时去掉ping目标主机。
2、远程复制时先判断目标文件系统是否包含增量复制基准快照，如果有则进行增量复制。
3、与远程复制相关的Web Service调用超时时间由3秒增加到10秒，并记录日志。
CDP分离器：
1、Linux分离器修改了磁盘镜像分析分区结构时未考虑Linux交换分区的Bug。
  目前可以正确分析的分区包括：EFI GPT,HPFS/NTFS,Extended,W95 Ext'd(LBA),Linux,Linux swap/Solaris。
2、Linux分离器修改了包含Linux Swap分区或EFI分区的磁盘做快速复制时解析分区信息出现的Bug
3、Windows分离器增加对OEM磁盘Vendor的识别。

20160615
基本功能：
1、出场检测程序增加获取磁盘通电时间及通电次数选项。
2、修正注册页面在注册包含未知授权码时页面异常，跳过未知授权码直接注册。
3、清理工程下test目录及其他无用文件。
SYNC模块：
1、修正快照级保护快照数量最小值为2个。
2、修正了一个可能导致一对多远程复制目标文件系统快照在一个目标删除失败后不删除其他目标快照的bug。
3、所有Web Service接口调用超时时间由30秒修改为3秒。
CDP模块：
1、修正记录级别保护不能正确删除过期CDP记录快照的bug。
2、增加远程复制时删除目标磁盘组记录级别CDP快照的功能。
3、开启磁盘组保护时，批量删除快照增加时间间隔200ms。

20160527
基本功能：
1、增加白牌资源文件。
2、增加达梦、金仓、Gbase8t备份代理授权。
3、修改页面字体为微软雅黑。
SYNC模块：
1、优化初始快照复制失败后，失败任务重试处理流程，提高重试效率。
CDP模块：
1、修改若干删除磁盘组、IO记录时导致页面卡死的空指针异常。
2、修改主机镜像恢复磁盘时发送恢复分区命令的bug。
CDP分离器：
1、Windows分离器修改CDP镜像管理界面上显示“正在恢复”。
2、Windows分离器CDP镜像通过Sanboot能够启动，再恢复到服务器磁盘上不能启动。

20160331
基本功能：
1、根据Smartctl输出判断磁盘是否损坏，解决存在磁盘损坏误报bug。
2、增加企业网盘注册授权判断，增加企业网盘树节点可跳转至企业网盘。
3、更新注册程序，增加企业网盘注册功能。
SYNC模块：
1、由于zfslib.so对于远程复制开启重删后增量快照发送有bug，暂时屏蔽远程复制重删功能。
Oracle模块：
1、放开添加网络服务名页面中数据库服务名的限制。
2、修改备份生成脚本页面中，表格只能列出6行数据的bug（只能显示6个表空间）。
3、修改添加通道时，点击保存后导致数据库对应通道混乱的bug。
4、定期自动清理RMAN标记过期的备份数据。
CDP分离器：
1、Linux分离器增加了对双机集群(HA)中的LVM卷的支持。
2、Linux分离器增加了对Multipath设备（磁盘和分区）的支持。

20160128
基本功能：
1、解决在jersey过滤器增加注册码判断后导致iSCSI相关restful接口无法访问的bug。
2、解决创建系统卷时频繁点击确认按钮导致多次创建javadb数据库的bug。
3、修改添加NTP服务器页面，输入框验证正则表达式。
SYNC模块：
1、对远程复制的数据流增加重复数据删除处理，对两边文件系统重删属性无要求，仅对数据流重删。
2、重构了WebService包路径，此版本不兼容以前版本，远程复制时需要两端版本一致。
NAS模块：
1、CIFS共享支持中文共享名。
2、屏蔽ecd用户。
Oracle模块：
1、解决“数据恢复”标签页添加通道时，保存后导致数据库对应通道混乱的bug。
CDP模块：
1、修改DllLoader代码中CDP_DISK_GROUP_INFO类LogRecordCount字段类型long。
2、修改DllLoader代码中CDPAPI_SNAPSHOT_OBJECT_INFO类增加long llLogIndexCount字段。
3、修改CDP子模块代码中CdpDiskGroupInfo类对应LogRecordCount类型为long，同步修改axis及restful变量。
4、创建磁盘时增加对磁盘容量是否超出卷组未用容量进行判断，并提示用户。
5、磁盘支持扩容操作，扩容容量必须大于原始容量。
6、添加SQLServer数据库名称允许数据库名称包含下划线。
CDP分离器：
1、解决Windows分离器在双机环境下识别磁盘序列号错误的bug。
2、解决Linux分离器在lun映射断开情况下客户机宕机的bug。
CDP驱动：
1、驱动修改了最大磁盘组数量(20改为2000)和最大磁盘数量(50改为5000)，受libiscsi限制最多创建256个target。

20151120
基本功能：
1、解决创建系统卷后可能会导致设备不重启的bug。
2、更新zfs动态库，解决zfs send -D参数无法关闭管道的bug。
SYNC模块：
1、解决设备远程复制授权用户名称形如test1时会列出test用户授权文件系统的bug。（grep -w）
2、解决添加复制任务时，目标文件系统含有一个快照时，不提示目标包含快照信息的bug。
3、解决开启远程复制后，自动快照会超出最大数目限制且不会自动删除的bug。
4、解决远程复制任务目标断电恢复供电后，设备启动但程序尚未启动完毕，源端进行重连发现没有数据缓存删除复制任务的bug（增加判断目标主机程序是否启动完毕）。
5、解决开启远程复制后，目标主机断网或断电导致源端同目标端进行快照一致性检查超时引起的自动快照间隔不断变长的bug。
6、解决远程复制失败后，重复添加相同任务到失败任务列表，导致远程复制任务发送间隔不断变长的bug。
7、解决远程复制关闭源主机后，目标主机中止复制，再次重启目标主机复制任务重现的bug。
8、解决远程复制目标主机断网时，源主机重启重建快照队列时会删除保留快照导致远程复制失败的bug。
NAS模块：
1、解决全局NFS页面在新版本Chrome浏览器中标点换行的bug。
Oracle模块：
1、解决Oracle RAC生成备份脚本错误的bug。（sequence值恢复语句不对，需要添加thread参数。）
2、解决Oracle RAC数据库通道页面数组越界造成页面无响应bug。
CDP分离器：
1、解决Linux分离器非快速复制时同步数据不一致的问题。

20150929
基本功能：
1、解决设备没有高级备份注册码时，升级页面不显示高级备份模块版本信息。
Oracle模块：
1、解决Oracle修改策略时，开始时间在当前时间之前，且开始时间是在1点-12点内，当前时间在13点-24点内，在这种情况下发送命令，下次执行时间为开始时间+12小时的bug。
2、解决Oracle修改策略时，在以下情况下：勾选周日和其他任意一天一起勾选时会产生跨过周日直接执行下一次策略的bug。

20150925
基本功能：
1、不再设置zfs_write_limit_max参数。
2、设备升级页面实现不同功能载荷统一升级，升级前显示模块更新日志。
3、增加Open API注册授权，持有Open API授权令牌可以访问iSCSI、CDP模块Restful API（需要更新注册程序XML）。
4、修正磁盘替换页面进行操作后等待时间过长的问题。
5、设备iSCSI模块提供Restful API。
6、网卡流量监控使用dTrace脚本监控，解决系统负载高时nicstat无法正确获取网卡流量的bug。
SYNC模块：
1、修改了一个可能导致远程复制线程意外终止的bug。
CDP模块：
1、设备CDP模块提供Restful API。
Oracle模块：
1、解决Oracle备份脚本不能正确删除归档日志的bug，生成脚本中增加nopromot关键字。
2、解决Oracle备份循环任务策略勾选最后一天后无法正确显示的bug。
3、解决Oracle备份循环任务策略修改保存后会立即执行的bug。
4、解决Oracle保存备份脚本后，每保存一次多执行一次RMAN脚本的bug。
5、解决Oracle备份脚本立即执行后页面中的Oracle备份执行时间和执行次数不更新，需要手动更新的bug。
6、解决Oracle删除备份任务后，被删除任务会抛出异常的bug。
7、解决Oracle备份策略中的开始时间置为空时，保存不报错，但无法正确执行的bug。
8、解决Oracle循环策略周围为25天，或者600小时，或者36000分钟以上时，会出现策略无法继续执行的bug。
9、解决Oracle备份策略，取消无限后保存，再次进入时，无法正确显示状态问题。
10、解决重启Tomcat后会立即执行一次Oracle备份的bug。
11、解决Oracle在发送任务时名重复，保存会替换掉原任务的bug。
12、解决Oracle发送任务时，策略选择周或月但未勾选任何一天，导致后异常，无法继续访问界面的bug。
备份模块：
1、系统管理中心、任务管理中心、参数配置程序在生成的JNLP文件中增加端口配置。
CDP分离器：
1、修正Linux分离器镜像写操作队列管理中存在的Bug，该Bug可能导致系统内存耗尽而死机。
2、修正Linux分离器删除大文件导致客户机宕机的bug（Mantis4595）。
3、修正Linux分离器设置db2数据库CDP镜像副本，数据库异常的bug（Mantis4608）。
4、修正Windows分离器在Windows2003下，如果开启了磁盘卷影副本服务导致Windows蓝屏的bug。

20150708
基本功能：
1、创建SYSVOL时调用/usr/msa/mba/Install.sh安装Web版MBA程序。
2、导入SYSVOL时执行ln -s /SYSVOL/SYSTEM/mba /usr/local/链接MBA到系统卷。
CDP模块：
1、修正CDP记录级别快照页面全部删除按钮换行的bug。
2、修正LVM中LV容量大于1TB，挂载到某目录下时，获取的分区信息中容量错误的bug。
CDP分离器：
1、修正Linux分离器快速复制过程中读取链接文件信息时出现读越界和未处理ACL(访问控制列表)数据块的Bug。
2、修正Linux分离器在磁盘或分区容量大于1TB时同步失败的Bug。
3、修正Windows分离器压力大的情况下可能会造成系统崩溃的问题。
4、Windows分离器配置文件msacdp.xml中增加<AsynMode>0</AsynMode>节点，0表示同步模式，1表示异步模式，如果需要提高性能，手动将AsynMode改为1。

20150618
基本功能：
1、修正若干对话框大小可以调整的bug。
2、设备backup目录更名为backups，解决设备和MBA web版目录同名冲突。
3、设备采用Web Service方式发布对外接口，不在监听1099端口（分离器与设备通信端口），解决设备和MBA web版端口冲突。
4、更新marsvtl驱动程序版本（版本:2015-05-25 17:33）。
5、整理/usr/msa目录下shell脚本及动态库文件权限，统一为755。
6、执行清理、检查卷组操作后，自动扫描磁盘状态。
7、修正设备CPU使用率不能正确显示的bug。
8、设备控制器节点主机选项卡增加配置设备端口（Tomcat端口）功能。
10、设备页面支持1250宽屏显示，调整若干页面组件大小。
11、修正设备磁盘状态页面误报导致亮红灯的bug。
12、设备备份功能节点增加VMware备份节点，点击后跳转到MBA web版VMware备份界面。
13、设备增加VMware高级备份功能注册授权，并修改相应注册码生成程序。
14、修正设备创建两个聚合网卡后再次登陆系统，点击网络管理节点页面崩溃的bug。
15、设备服务端程序不再设置/etc/system文件中zfs_arc_max、zfs_write_limit_max、zfs_txg_timeout三个参数。
16、修正部分版本Chrome浏览器无法弹出“关于”信息的bug。
CDP模块：
1、CDP磁盘转换虚拟磁盘，增加磁盘转换任务页面，显示磁盘转换进度及添加、删除磁盘转换任务。
2、CDP磁盘转换虚拟磁盘到VM及NAS目录，目标目录存在正在进行的磁盘转换任务时，不能删除目标目录。
3、修正CDP磁盘转换虚拟磁盘日志记录错误的bug。
4、修正CDP磁盘转换虚拟磁盘未及时将缓冲区数据写入磁盘的bug，提高写入性能。
5、设备CDP主机镜像节点增加高级镜像代理下载功能，包含Windows、Linux、AIX、HP-UX、Solaris版本代理及Windows、Linux镜像代理升级补丁。
6、设备CDP主机镜像节点增加Windows、Linux、AIX、HP-UX、Solaris个平台JRE6下载（JRE存放在/usr/msa/cdp/jre目录下）。
7、设备CDP磁盘组在开启快照级保护进行复制过程中，不停止复制也可以创建磁盘组副本。
CDP分离器：
1、Linux分离器增加了对2.6.18内核虚拟化(xen)版本的支持，修改了数据恢复的处理流程，监控恢复过程中源盘数据的变化。
2、Linux分离器增加了对双机集群(HA)的支持，即使用命令行方式启动和停止CDP。
3、Linux分离器增加了对Debian 7.8(内核3.3.3)版本的支持。
4、Linux分离器修正了安装和卸载脚本中获取Debian版本出现错误的Dug。
5、Linux分离器修正了恢复LVM卷出现错误及Debian系统下解析df命令错误的Bug。。
6、一致性代理采用Web Service与设备通信，发送一致性点，创建一致性代理快照。
7、修正一致性代理数据库连接参数设置后无法再次修改的bug。
8、修正设备有多个网段IP地址时，一致性代理无法正确连接到设备指定IP地址造成无法创建一致性快照的bug。
注意：更新此版本设备后，必须重新安装新版本分离器或打升级补丁，否则无法创建一致性代理快照。

20150403
基本功能：
1、解决状态页面SATA磁盘温度显示为1摄氏度的bug（20150313之后版本出现此问题）。

20150401
基本功能：
1、初始化Expander时增加Rev字段的获取，结合原有Product字段动态读配置文件。

20150318
基本功能：
1、文件系统属性页面设置压缩级别，下拉框内容在未启用压缩时显示高效选项。
CDP模块：
2、解决设备没有NAS及VM目录时，虚拟磁盘页面异常的bug。

20150313
基本功能：
1、注册码页面中设备中功能“Oracle ExaData功能”修改为“Oracle高级备份功能”。
2、更改创建卷组时容量授权计算方式，防止用户突破总授权容量。
3、通过phpVirtualBox创建的虚拟机默认路径更改为/SYSVOL/VM。
4、文件系统压缩级别变更为中等(gzip6)、高等(gzip9)、高效(lz4)。(lz4压缩算法需要更改烧dom盘脚本，加入/usr/bin/lz4，并给予执行权限)
5、Smartctl验证SATA磁盘时增加参数-d sat,12。
6、重启设备时用户可以选择是否初始化快照数据库，如果选择初始化数据库则初始化完毕后才能访问设备页面。
7、创建、导入系统卷重启时，强制初始化快照数据库。导入卷组时，初始化该卷组中文件系统对应的数据库表。
8、修改升级安装包中Server.xml文件内容，确保用户升级安装后强制初始化快照数据库。
9、初始化Expander时增加Rev字段的获取，结合原有Product字段读配置文件，更新/usr/msa/model目录下配置文件。
10、没有系统卷的时候允许更新msa.tar.gz，更新包传到tmp目录下，更新完成后删除msa.tar.gz。
SYNC模块：
1、修正文件系统不存在复制任务时，手工快照过期后没有自动清除的bug。
2、远程复制压缩级别变更为中等(gzip6)、高等(gzip9)、高效(lz4)。
3、采用数据库存储文件系统（NAS、VTL、CDP）快照信息，避免快照数量过多时获取快照时间过长。
CDP模块：
1、根据现有需求重写“磁盘组”及“CDP保护”节点服务端接口，大幅度提高节点展开速度。
2、更新CDP驱动程序版本，当磁盘组初始化失败时，从数据库中删除该磁盘组的信息。
3、主机镜像节点与分离器通信，分离器返回错误信息时，增加错误码提示。
4、恢复数据(反向同步)页面增加停止恢复按钮，用户必须手工停止恢复。
5、修改磁盘LU导入及创建判断方式，防止重复导入及多次创建造成的bug。
6、记录级别创建副本失败后，自动将副本删除保证zfs与驱动一致性。
7、启动设备时必须等待CDP驱动程序初始化完毕后才能访问web页面。
8、导入系统卷后停止磁盘组快照级别保护，防止没有离线再次上线磁盘组致使stmf中没有磁盘信息，保护无法手工停止且不能进行离线、上线操作。
9、虚拟磁盘转换放开VDI和VHD格式磁盘2TB容量限制。
10、主机镜像磁盘详细信息页面中“系列号”修改为“序列号”。
CDP分离器：
1、修正记录级别添加了oracle代理保存删除后，再添加SQL代理保存后重新进入记录界面，显示两个代理的bug。
2、修正后台打印的连接数据库异常，SQLServer代理检查连接时，连接成功后关闭测试连接。
出厂检测程序：
1、Smartctl验证SATA磁盘时增加参数-d sat,12。
2、增加对设备16Gb光纤卡的检测并修改对应配置文件。
OEM设备：
1、修改同有、天融信登陆logo及部分图片。

20150113
CDP分离器：
1、更新20150109版Windows分离器。修正同步完成之后在保护过程中有可能引发蓝屏，某些软raid不能成功保护。

20150107
CDP模块：
1、修正OEM版设备主机镜像节点Linux主机，磁盘、分区和卷设置镜像无法获取目标设备。

20150106
基本功能：
1、用户组增加marsauth组，用于存储远程复制授权用户。
2、更新VTL动态库代码，增加返回marsauth组用户信息。
3、用户页面增加添加授权用户及列出授权用户功能。
4、文件系统属性设置页面，增加对文件系统设置远程复制授权用户属性。
5、文件系统为复制目标时不能更改授权信息，删除授权用户时如果该用户已经授权给文件系统则不能删除此用户。
6、恢复出厂设置时用户可选择备份当前网络配置信息。
7、注册码文件迁移到/usr/msa目录下，恢复出厂设置时拷贝注册码文件到/syspool/boot/目录，tomcat启动脚本启动时将注册码文件从/syspool/boot/移动到/usr/msa（需要更改dom盘制作脚本）。
8、修正重新制作dom盘后，因hostid变化导致原有卷组导入失败的bug。
9、更新/usr/msa下model目录及pre_start.sh脚本。
NAS模块：
1、修正CIFS设置页面，Windows中文账户添加ID映射后用户名乱码、无法删除Windows中文账户映射的问题。
2、修正domain配置文件为空时，页面异常bug。
SYNC模块：
1、远程复制页面增加弹出连接远程设备对话框，登陆成功后列出授权用户的文件系统，匿名登陆成功后列出未授权的文件系统。
2、修正目标端中止复制后，源端复制任务没有删除的bug。
3、修正目标端中止复制后，源端CDP保护提示存在复制任务，保护无法停止的bug。
Oracle模块：
1、/usr/msa目录下增加oracle_config文件用于配置环境变量，生成脚本前读取。
2、修正RMAN日志中文乱码bug。
VM模块：
1、设备启动时自动判断是否有虚拟机注册码，如果没有移除phpVirtualbox管理程序，防止未授权访问。
CDP模块：
1、Oracle一致性代理创建快照默认时间由3秒变为60秒。
2、设备更新CDP驱动，修改了恢复磁盘签名时未考虑GPT分区的Bug。
3、修正磁盘组远程复制功能，目标端磁盘组不显示停止复制按钮的bug。
4、修正快照级别磁盘组的快照点创建副本时，在弹出的是否立即查看副本的对话框中点击“否”后，页面崩溃无法显示的bug。
5、修正磁盘组的远程复制，从目标端停止复制任务后，源端的磁盘组标志位未清空导致无法停止CDP的bug。
6、修正记录级别被保护主机重启后，磁盘组中数据库代理信息丢失的bug。
7、修正CDP磁盘转化虚拟磁盘成功后未刷新磁盘LU缓存，导致页面显示LU离线的bug。
CDP分离器：
1、更新20141216版Windows分离器，修正部分bug。
2、更新20141219版Linux分离器，修改了快速同步时Hook磁盘写操作的处理流程及重启后没有继续保护的bug。
3、一致性代理增加重连数据库功能，被保护主机数据库服务及监听服务未启动，导致的连接数据库失败时间隔30秒重新连接数据库。

20141124
基本功能：
1、/var/msa下*.sh及*.xml迁移到/usr/msa下(target及initiator密码需要重新配置)。
2、优化日志查询页面查询速度。
NAS模块：
1、修改加入Windows2008域控后无法显示已加入的bug。
VM模块：
1、程序不在生成/etc/rc0.d/K83VboxWebSrv.sh、/etc/rc1.d/K83VboxWebSrv.sh、/etc/rcS.d/K83VboxWebSrv.sh、/etc/rc2.d/S83VboxWebSrv.sh文件，设备服务启动时启动virtualbox/webservice服务
CDP模块：
1、提高CDP磁盘转换虚拟磁盘的速度。
2、优化记录级别快照列表页面相应速度。
3、解决分配CDP磁盘lun号错误的bug。
4、修改快照级别同步目标磁盘组停止复制后，上线开启保护后台异常的bug。
CDP分离器：
1、更新Linux分离器，对快速复制读写速度进行了优化，分区文件碎片化程度越小效果越明显。

20141113
基本功能：
1、卷组页面导入卷组功能，禁止导入系统卷。
2、卷组页面增加重复数据删除比率显示。
CDP模块：
1、修改同一卷组下磁盘组名称部分匹配（test与test1）时创建副本导致磁盘组无法删除的bug。
2、修改LVM下分区设置镜像时，不管对那个分区设置镜像，“源分区”获取的都是“backup”分区的bug。
CDP分离器：
1、更新Windows分离器，修改同一台机器多个保护会出现蓝屏。
出厂检测程序：
1、增加列出磁盘厂商、型号、固件版本。

20141031
基本功能：
1、卷组页面增加导入卷组功能，实现卷组导入并导入相应的复制配置信息。
2、创建及导入系统卷时将VirtualBox配置信息链接到系统卷。
3、增加同有、天融信、东华OEM程序。
4、更新marstor帮助手册。
Oracle模块：
1、放开RMAN通道路径限制，允许输入UNC格式网络服务器路径。
2、修改RAMN备份任务页面，任务名在页面跳转后丢失的bug。
3、修改RAMN备份任务页面，任务名及脚本内容为空时提示信息错误的bug。
4、修改RMAN备份策略页面，循环任务周期可以为0的bug。
CDP模块：
1、主机镜像节点页面、快照列表页面、设置CDP保护级别页面修改。
2、添加磁盘时可以输入磁盘名，记录级别保护可以生成手工快照。
3、磁盘页面增加磁盘转换为虚拟磁盘功能,虚拟磁盘容量最大2TB，磁盘组中磁盘最大数量为10。
4、更新CDP动态库及marscdpadm。
CDP分离器：
1、更新Linux分离器，解决快速复制问题。
2、更新Windows分离器，解决bug。
   虚拟机取不到序列号，将虚拟磁盘的签名作为序列号。
   对数据保护性能优化，以异步方式实现数据保护。提升I/O性能

20141017
基本功能：
1、默认网关可以设置为空(删除defaultroter文件)。
2、解决主机组添加成员(设备管理>主机组>iSCSI>成员>添加)，成员格式错误时后台抛异常的问题
3、目标器别名放开必须是Mars-iSCSI-T-开头的限制，解决OEM无法创建目标器问题。
4、OEM资源文件增加OEM厂商官网链接。
CDP模块：
1、可以创建容量为999999GB的磁盘。
2、主机端口限制为1~65535。
CDP分离器：
1、更新Windows分离器
   虚拟机取不到序列号，将虚拟磁盘的签名作为序列号。
   对数据保护性能优化，以异步方式实现数据保护。提升I/O性能

20141014
基本功能：
1、更新帮助文档
CDP模块：
1、优化CDP保护页面列出复制任务操作，提高程序响应速度。
2、优化CDP添加本地复制页面添加复制任务操作，提高程序相应速度。
3、优化CDP添加远程设备操作，提高程序相应速度。
4、解决Windows平台正在恢复数据时跳出恢复界面，点击该Windows主机的连按按钮，没有跳转到恢复数据界面的bug。
NAS模块：
1、设备不在域中时，禁止添加ID映射。
2、添加ID映射页面禁止输入域用户名称，只能从下拉列表中选择域用户。

20141010
SYNC模块：
1、解决自动快照数量超出设置最大数量后不删除早期快照的bug。
CDP模块：
1、优化CDP保护页面，提高程序响应速度。
2、解决记录级别复制完成后，目标端磁盘组回滚失败的bug。
3、解决磁盘容量大于1TB创建的副本，通过Microsoft iSCSI发起程序连接后，副本容量显示为1GB的bug。

20140926
备份模块：
1、备份功能>统一备份>系统维护 页面容量单位增加B
CDP模块：
1、修正后台打印的网络连接异常

20140925
基本功能：
1、出厂检测程序增加状态页面是否显示磁盘Tab的设置选项(e选项依次设置磁盘类型、盘位数、是否显示磁盘表格)
CDP模块：
1、没有CDP注册码时不显示主机镜像表格
VM模块：
1、修正删除IDE及SCSI控制器下硬盘时，无法显示验证码的bug。
CDP分离器：
1、更新Linux分离器

20140919
基本功能：
1、恢复出场设置界面删除提示测试信息

20140918
基本功能：
1、删除友备OEM系统管理>状态>磁盘界面存在的黑线

20140917
基本功能：
1、修改打包编译脚本删除ROOT.war中资源文件OEM相关目录
2、更新友备OEM帮助文档

20140916
基本功能：
1、修改打包编译脚本删除ROOT.war中test目录

20140915
基本功能：
1、增加友备OEM
SYNC
1、修改远程复制脚本

20140829
基本功能：
1、更新帮助文档
2、修改内存图表显示bug

20140822
基本功能
1、增加恢复出厂设置的功能
2、去掉msatransfer服务，改为webservice实现
CDP
1、增加记录级别副本回滚的功能，副本直接上线
2、修改了主机镜像，恢复数据产生的bug

20140731
CDP
1、修改直接挂载副本和回滚CDP快照数据不一致的问题
2、增加副本中的记录回滚功能

20140724
更新帮助文档

20140715
修改bug

20140714
修改bug

20140710
设备复制
1、复制过程中，设备源端重启，自动继续复制
2、复制过程中，可以更改目标机器ip
3、目标机器支持使用主机名，需要DNS支持或者更改/etc/hosts文件
CDP分离器
更新分离器

20140625
修改暂停复制的问题

20140618
修改bug

20140616
修改bug

20140612
cdp分离器
1.在选择快速同步情况下，修复逻辑分区不能正常显示的bug.

20140610
cdp分离器
1逻辑分区同步失败(快速同步)

20140609
cdp分离器
1如果硬盘有坏块的情况下，快速同步失败，但块同步成功。

20140604
1、不再修改/etc/hosts
cdp分离器
如果硬盘有坏块的情况下，快速同步失败，但块同步成功。

20140603
1、更新帮助文档

20140529
1、无法启动MBA程序的bug
2、CDP LUN映射bug

20140521
1、去除/SYSVOL/SYSTEM/tomcat

20140519
1、创建DCA-DATA
2、解决创建/MSA的问题

20140514

20140513

20140512
1、CDP分离器OEM相关改动

20140507 